/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["github.com", "res.cloudinary.com", "lh3.googleusercontent.com"],
      },
};

export default nextConfig;
